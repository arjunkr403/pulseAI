db.query(...)      → Build the SQL query

.filter(...)       → Add WHERE

.order_by(...)     → Add ORDER BY

.limit(...)        → Add LIMIT

.all()             → Execute and return all matching rows

.first()           → Execute and return the first matching row

.one()             → Execute and expect exactly one row